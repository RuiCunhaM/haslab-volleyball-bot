environment: {}
parameters: {}
packages:
  - name: volley
    environment: {}
    parameters: {}
    annotations: {}
    functions:
      - name: create-poll
        binary: false
        main: ""
        runtime: "python:3.11"
        web: true
        parameters: {}
        environment:
          SECRET_TOKEN: "${SECRET_TOKEN}"
          DISCORD_HOOK: "${DISCORD_HOOK}"
          CL_COLLECTION: "${CL_COLLECTION}"
        annotations: {}
        limits:
          timeout: 30000
